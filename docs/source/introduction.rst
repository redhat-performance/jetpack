Introduction
============

JetPack is the easiest way to deploy Director/Tripleo on baremetal. JetPack will install OpenStack using infrared on a set of homegnous servers on Red Hat's lab i.e scalelab and also on alias lab if you boot it to second interface. User can run JetPack from their laptop or an ansible jump host.
